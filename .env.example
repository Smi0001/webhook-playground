DATABASE_URL=postgresql://postgres:password@localhost:5432/webhook_microservice
PORT=3000
# Public-facing base URL used in the UI to construct webhook URLs
BASE_URL=http://localhost:3000
# Sub-path prefix when app is served under a nested route (e.g. /webhook)
# Leave empty for root deployment. Must match the Nginx location prefix.
# BASE_PATH=/webhook
